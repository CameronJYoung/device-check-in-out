export default class NewUser{
	constructor(first,last,phone,email,password,password2) {
		this.first = first,
		this.last = last,
		this.phone = phone,
		this.email = email,
		this.password = password
		this.password2 = password2
	}
}
